#include <stdio.h> 
// stdio: 출력을 시킬 수도 있고, 저장하라는 명령을 함(읽고 쓰기)/지우면 printf에 빨간 표시가 뜬다(실행 못함)
// stdio.h는 파일의 내용을 이 위치에 가져다 놓으라는 뜻
//printf 함수의 호출을 위해 필요한 문장
// stdio.h파일에는 printf함수호출에 필요한 정보 존재

int main()//우리가 프로그램을 실행할 때 가장 먼저 찾는 것
{
	//<<Hello World>>

	printf("Hello World\n"); 

	//;은 문장의 끝을 표현하기 위한 문자
	// printf함수 (정확히는 명령어)
	// 출력하는 형식을 맘대로 지정 할 수 있음.

	return 0; //0는 정상 종료/컴퓨터에게 종료를 알려줌./ 함수를 호출한 영역으로 값을 전달
}
