int main()
{
	int a, b;
	printf("a,b=");
	scanf_s("%d %d", &a, &b);//두 정수 a,b를 입력받는다.

	for (;b!= 0;) {//b가 0이 아니면 반복한다.

		int temp = b; //b값을 미리 저장해야 a에 나중에 저장가능
		b = a % b;
		a = b; 
		//매 반복마다 a%b(a와b를 나눈 나머지)를 구하고 
		// 그 값을 b로 저장한다. 기존 b값은 a로 옮겨진다.
		// 반복하다 b가 0이되면 반복종료.
		//a값에는 최대공약수가 저장된다.

	}
	printf("%d\n", a);
	return 0;*/
    }
